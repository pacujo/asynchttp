#!/bin/bash

ARCH=$1

flags=(
    --cc=@CC@
    -Iinclude
    -D_FILE_OFFSET_BITS=64
)

libconfig_flags=$(@CONFIG_PARSER@ |
    tr ' ' '\n' |
    grep '^-[DI]')

exec @FSTRACECHECK@ "${flags[@]}" $libconfig_flags stage/$ARCH/build/src/*.c
